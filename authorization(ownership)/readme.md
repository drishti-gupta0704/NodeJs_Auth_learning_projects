
# Node.js Authorization (Ownership Based)
📌 Project Overview

This project demonstrates Authentication and Authorization using Node.js, Express, JWT, and MongoDB.

It implements Ownership-Based Authorization, meaning users can only access or modify resources that belong to them.

🚀 Features
- User Authentication using JWT
- Password hashing using bcrypt
- Protected routes using middleware
- Ownership-based authorization
- Environment variable security using dotenv
- Modular project structure